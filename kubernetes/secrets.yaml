# Template — replace with sealed-secrets or external secrets manager in production
apiVersion: v1
kind: Secret
metadata:
  name: retail-secrets
  namespace: retail-intel
type: Opaque
stringData:
  POSTGRES_PASSWORD: "CHANGE_ME"
  GRAFANA_ADMIN_PASSWORD: "CHANGE_ME"
